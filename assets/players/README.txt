Place player avatar images here (e.g. lol-faker.png). Reference them via the "avatar" field in data/players.json.
When "avatar" is empty, the app auto-generates a colored initials badge, so images are fully optional.
